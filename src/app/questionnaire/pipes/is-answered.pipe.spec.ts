import { IsAnsweredPipe } from './is-answered.pipe';

describe('IsAnsweredPipe', () => {
  it('create an instance', () => {
    const pipe = new IsAnsweredPipe();
    expect(pipe).toBeTruthy();
  });
});
